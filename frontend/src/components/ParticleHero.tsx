'use client';

import React from 'react';

const ParticleHero = () => {
  return (
    <div className="absolute inset-0 z-0 bg-[#121c2e]">
      {/* Static background to replace Three.js animation */}
    </div>
  );
};

export default ParticleHero;