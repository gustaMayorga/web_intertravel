'use client';\n\nimport { useState } from 'react';\nimport Link from 'next/link';\nimport { motion, AnimatePresence } from 'framer-motion';\n\nconst MobileNavigation = () => {\n  const [activeTab, setActiveTab] = useState('home');\n\n  const navigationItems = [\n    {\n      id: 'home',\n      label: 'Inicio',\n      icon: '🏠',\n      href: '/',\n      color: 'text-intertravel-gold'\n    },\n    {\n      id: 'packages',\n      label: 'Paquetes',\n      icon: '✈️',\n      href: '/paquetes',\n      color: 'text-blue-500'\n    },\n    {\n      id: 'trips',\n      label: 'Mis Viajes',\n      icon: '🧳',\n      href: '/mobile/trips',\n      color: 'text-green-500'\n    },\n    {\n      id: 'profile',\n      label: 'Perfil',\n      icon: '👤',\n      href: '/mobile/profile',\n      color: 'text-purple-500'\n    },\n    {\n      id: 'more',\n      label: 'Más',\n      icon: '⚙️',\n      href: '/mobile/menu',\n      color: 'text-gray-500'\n    }\n  ];\n\n  return (\n    <>\n      {/* Bottom Navigation */}\n      <div className=\"fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 z-50 safe-area-bottom\">\n        <div className=\"flex justify-around items-center py-2\">\n          {navigationItems.map((item) => (\n            <Link\n              key={item.id}\n              href={item.href}\n              onClick={() => setActiveTab(item.id)}\n              className=\"flex flex-col items-center py-2 px-3 min-w-0 relative\"\n            >\n              <motion.div\n                className={`text-2xl mb-1 transition-all duration-300 ${\n                  activeTab === item.id ? item.color : 'text-gray-400'\n                }`}\n                whileTap={{ scale: 0.9 }}\n                animate={{\n                  scale: activeTab === item.id ? 1.1 : 1\n                }}\n              >\n                {item.icon}\n              </motion.div>\n              \n              <span className={`text-xs font-medium transition-all duration-300 ${\n                activeTab === item.id ? item.color : 'text-gray-400'\n              }`}>\n                {item.label}\n              </span>\n              \n              {/* Active indicator */}\n              <AnimatePresence>\n                {activeTab === item.id && (\n                  <motion.div\n                    initial={{ scale: 0 }}\n                    animate={{ scale: 1 }}\n                    exit={{ scale: 0 }}\n                    className={`absolute -top-1 w-1 h-1 rounded-full ${item.color.replace('text-', 'bg-')}`}\n                  />\n                )}\n              </AnimatePresence>\n            </Link>\n          ))}\n        </div>\n      </div>\n\n      {/* Safe area for content */}\n      <div className=\"h-20\" />\n    </>\n  );\n};\n\nexport default MobileNavigation;\n